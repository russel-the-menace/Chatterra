diff --git a/node_modules/react-native/Libraries/Text/TextInput/Multiline/RCTUITextView.mm b/node_modules/react-native/Libraries/Text/TextInput/Multiline/RCTUITextView.mm
index 6e9c384..3c00f6d 100644
--- a/node_modules/react-native/Libraries/Text/TextInput/Multiline/RCTUITextView.mm
+++ b/node_modules/react-native/Libraries/Text/TextInput/Multiline/RCTUITextView.mm
@@ -33,7 +33,15 @@ static UIColor *defaultPlaceholderColor(void)
   // Default placeholder color from UITextField.
   return [UIColor placeholderTextColor];
 }
-
+static UIMenu *RCTSuppressedEditMenu(void)
+{
+  // iOS 16 treats a genuinely empty menu as a request for its default suggestions.
+  // A hidden child keeps the menu hierarchy nonempty without rendering an action.
+  UIAction *hiddenAction = [UIAction actionWithHandler:^(__unused UIAction *action) {
+  }];
+  hiddenAction.attributes = UIMenuElementAttributesHidden;
+  return [UIMenu menuWithChildren:@[ hiddenAction ]];
+}
 - (instancetype)initWithFrame:(CGRect)frame
 {
   if (self = [super initWithFrame:frame]) {
@@ -295,6 +303,21 @@ static UIColor *defaultPlaceholderColor(void)
 
 #pragma mark - Context Menu
 
+#if defined(__IPHONE_OS_VERSION_MAX_ALLOWED) && __IPHONE_OS_VERSION_MAX_ALLOWED >= 160000
+- (UIMenu *)editMenuForTextRange:(__unused UITextRange *)textRange
+                 suggestedActions:(__unused NSArray<UIMenuElement *> *)suggestedActions API_AVAILABLE(ios(16.0))
+{
+  if (_contextMenuHidden) {
+#if DEBUG
+    NSLog(@"ChatterraTextMenu: suppressing iOS edit menu");
+#endif
+    return RCTSuppressedEditMenu();
+  }
+
+  return [super editMenuForTextRange:textRange suggestedActions:suggestedActions];
+}
+#endif
+
 - (BOOL)canPerformAction:(SEL)action withSender:(id)sender
 {
   if (_contextMenuHidden) {
@@ -304,6 +327,15 @@ static UIColor *defaultPlaceholderColor(void)
   return [super canPerformAction:action withSender:sender];
 }
 
+- (id)targetForAction:(SEL)action withSender:(id)sender
+{
+  if (_contextMenuHidden) {
+    return nil;
+  }
+
+  return [super targetForAction:action withSender:sender];
+}
+
 - (void)buildMenuWithBuilder:(id<UIMenuBuilder>)builder
 {
 #if defined(__IPHONE_OS_VERSION_MAX_ALLOWED) && __IPHONE_OS_VERSION_MAX_ALLOWED >= 170000
@@ -315,6 +347,15 @@ static UIColor *defaultPlaceholderColor(void)
 #endif
 
   [super buildMenuWithBuilder:builder];
+
+  if (_contextMenuHidden) {
+    // iOS 26 adds system suggestions such as Search Web after action filtering.
+    // Empty the final root menu so no text-selection actions survive that pass.
+    [builder replaceChildrenOfMenuForIdentifier:UIMenuRoot
+                              fromChildrenBlock:^NSArray<UIMenuElement *> *(NSArray<UIMenuElement *> *children) {
+                                return @[];
+                              }];
+  }
 }
 
 #pragma mark - Dictation
diff --git a/node_modules/react-native/Libraries/Text/TextInput/RCTBackedTextInputDelegateAdapter.mm b/node_modules/react-native/Libraries/Text/TextInput/RCTBackedTextInputDelegateAdapter.mm
index 82d9a79..781a48c 100644
--- a/node_modules/react-native/Libraries/Text/TextInput/RCTBackedTextInputDelegateAdapter.mm
+++ b/node_modules/react-native/Libraries/Text/TextInput/RCTBackedTextInputDelegateAdapter.mm
@@ -224,6 +224,33 @@ static void *TextFieldSelectionObservingContext = &TextFieldSelectionObservingCo
 
 #pragma mark - UITextViewDelegate
 
+#if defined(__IPHONE_OS_VERSION_MAX_ALLOWED) && __IPHONE_OS_VERSION_MAX_ALLOWED >= 160000
+- (UIMenu *)textView:(__unused UITextView *)textView
+    editMenuForTextInRange:(__unused NSRange)range
+         suggestedActions:(__unused NSArray<UIMenuElement *> *)suggestedActions API_AVAILABLE(ios(16.0))
+{
+  // iOS 16 suggested actions bypass `canPerformAction:`, so hide the entire modern edit menu too.
+  if (_backedTextInputView.contextMenuHidden) {
+    return [UIMenu menuWithChildren:@[]];
+  }
+
+  return nil;
+}
+#endif
+
+#if defined(__IPHONE_OS_VERSION_MAX_ALLOWED) && __IPHONE_OS_VERSION_MAX_ALLOWED >= 260000
+- (UIMenu *)textView:(__unused UITextView *)textView
+    editMenuForTextInRanges:(__unused NSArray<NSValue *> *)ranges
+          suggestedActions:(__unused NSArray<UIMenuElement *> *)suggestedActions API_AVAILABLE(ios(26.0))
+{
+  if (_backedTextInputView.contextMenuHidden) {
+    return [UIMenu menuWithChildren:@[]];
+  }
+
+  return nil;
+}
+#endif
+
 - (BOOL)textViewShouldBeginEditing:(__unused UITextView *)textView
 {
   return [_backedTextInputView.textInputDelegate textInputShouldBeginEditing];
diff --git a/node_modules/react-native/scripts/cocoapods/new_architecture.rb b/node_modules/react-native/scripts/cocoapods/new_architecture.rb
index 7d8f8e6..c4bd054 100644
--- a/node_modules/react-native/scripts/cocoapods/new_architecture.rb
+++ b/node_modules/react-native/scripts/cocoapods/new_architecture.rb
@@ -30,6 +30,10 @@ class NewArchitectureHelper
         end
 
         installer.target_installation_results.pod_target_installation_results.each do |pod_name, target_installation_result|
+            # Xcode 26 rejects fmt 11's consteval implementation under C++20.
+            # Keep fmt's podspec-selected C++17 while React Native remains on C++20.
+            next if pod_name == "fmt"
+
             target_installation_result.native_target.build_configurations.each do |config|
                 config.build_settings[cxxBuildsettingsName] = Helpers::Constants::cxx_language_standard
             end
diff --git a/node_modules/react-native/third-party-podspecs/fmt.podspec b/node_modules/react-native/third-party-podspecs/fmt.podspec
index 2f38990..4699c24 100644
--- a/node_modules/react-native/third-party-podspecs/fmt.podspec
+++ b/node_modules/react-native/third-party-podspecs/fmt.podspec
@@ -18,7 +18,9 @@ Pod::Spec.new do |spec|
     :tag => "11.0.2"
   }
   spec.pod_target_xcconfig = {
-    "CLANG_CXX_LANGUAGE_STANDARD" => rct_cxx_language_standard(),
+    # Xcode 26's Apple Clang rejects fmt 11's consteval checks under C++20.
+    # fmt is ABI-compatible here and compiling this pod as C++17 disables that path.
+    "CLANG_CXX_LANGUAGE_STANDARD" => "c++17",
     "GCC_WARN_INHIBIT_ALL_WARNINGS" => "YES" # Disable warnings because we don't control this library
   }
   spec.platforms = min_supported_versions
