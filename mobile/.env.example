# Production API. Override this only when developing against another backend.
EXPO_PUBLIC_API_URL=https://api.feiwan.online

# Web and mobile must use the same value to share conversations, memories,
# relationship state, and contact preferences.
EXPO_PUBLIC_USER_ID=YOUR_SHARED_USER_ID
